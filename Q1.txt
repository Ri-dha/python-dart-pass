Because of the use of the keyword const it means the variable hour can't be Updated,
so it means it's going to be the same as assigned as the value at compile time.
That means we can't use const DateTime.now() for it's going to always be updated
